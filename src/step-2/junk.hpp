dealii::SmartPointer<T, P>& dealii::SmartPointer<T, P>::operator=(T*) [with T = const dealii::SparseMatrix<double>; P = void] <near match>


  SmartPointer<T,P> &SmartPointer<T,P>::operator = (T *tt)


const dealii::SmartPointer<const dealii::SparseMatrix<double> >*’
  to ‘dealii::SmartPointer<const dealii::SparseMatrix<double> >*’

const dealii::SmartPointer<const dealii::SparseMatrix<double>, heat::MatrixChain<dealii::SparseMatrix<double> > >’
const dealii::SparseMatrix<double>*’)

dealii::SmartPointer<const dealii::SparseMatrix<double>, heat::MatrixChain<dealii::SparseMatrix<double> > >::SmartPointer(const dealii::SparseMatrix<double>&)’

heat::MatrixChain<T, Trest ...>::MatrixChain(const T&, const Trest& ...) [with T = dealii::SparseMatrix<double>; Trest = {}]
 MatrixChain<T, Trest...>::MatrixChain(const T &A_in, const Trest & ... rest)
 ^
